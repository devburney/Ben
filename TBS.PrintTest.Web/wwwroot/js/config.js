var CONFIG = CONFIG ? (function () {})() : {
    // Email address to be contacted in the case of an error (e.g. JavaScript, page unavailable etc.).
    SHARED_MAILBOX: "do-not-reply@tbs-sct.gc.ca",

    // Virtual path to the application (e.g. "/application").
    // Note: Leave empty (i.e. "") if the application runs from the root of the domain.
    ROOT: ""
};